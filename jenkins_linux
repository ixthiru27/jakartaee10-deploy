    agent any

    tools {
        maven 'local_maven'
        jdk 'java25'
    }

    environment {
        TOMCAT_WEBAPPS = "/root/apache-tomcat-9.0.115/webapps"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }
}

